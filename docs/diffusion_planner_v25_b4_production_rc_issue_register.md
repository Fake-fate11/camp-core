# V25 B4 production RC issue register

This register is frozen for the single B4 production-RC engineering stage.  It
does not add a scientific gate or change the fixed DP, K8, trajectory, atom,
model, threshold, NI, multiplicity, evaluation, or claim contracts.

| ID | Priority | Failure signature | Required closure | Status |
| --- | --- | --- | --- | --- |
| B4-RC-001 | P0 | `validate_v25_zero_overlap()` body became unreachable after the B3 validator definition | Restore independent function bodies and direct/caller tests | implementation and local regression complete; AutoDL suite pending |
| B4-RC-002 | P0 | Real candidate0 native receipt and pool consumer disagree on required K8 fields | One versioned actual-native ABI with distinct candidate0 primary/supplementary branches | implementation and ABI mutations complete; production canary pending |
| B4-RC-003 | P0 | Producer and reviewer share a projector/helper and can self-confirm the same defect | Reviewer reads the ABI declaration but independently parses and recomputes evidence | independent parser/recompute implemented; production canary pending |
| B4-RC-004 | P0 | Prior production preflight did not instantiate every real native branch | Same production entrypoints, raw callback, three scenario classes, 3 arms x 64 ticks | authority/plan/certificate implemented; production canary pending |
| B4-RC-005 | P0 | Release reservation and first scientific exposure are conflated | Separate operational-attempt and scientific-exposure ledgers with atomic transitions | implementation and state-machine mutations complete; production canary pending |
| B4-RC-006 | P1 | A renamed source family can be mistaken for independent provenance | Record common generator provenance; prove independence using geometry/export/semantic clone hashes | implementation and local regression complete; B4 pre-open review pending |
| B4-RC-007 | P1 | Native field access can outgrow the ABI without a failing test | Static consumer inventory plus required/extra/type/shape/cross-branch mutations | implementation and local ABI regression complete; AutoDL suite pending |
| B4-RC-008 | P1 | Raw receipt or tensor evidence can be lost before projection failure | Persist and seal raw/preprojection evidence before comparison/projection | implementation and independent mutation regression complete; production canary pending |
| B4-RC-009 | P1 | Independently selected real signal branches can share one route identity and violate the route-asset denominator | Select the lexicographically first route-distinct real fixture combination and reject an unavailable combination | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-010 | P1 | Production-RC wrapper accepted a canary CAS root but delegated it to a legacy validator that only permits the live canonical CAS | Validate the discarded legacy field against its canonical placeholder while binding all production-RC operational/scientific ledgers to the caller's exact isolated CAS root | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-011 | P0 | The actual native arm callback validated a production-RC release/exposure receipt with the legacy holdout release/consumption validators | Dispatch on the frozen release schema and normalize only the common pre-forward one-time-state predicates | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-012 | P0 | The mapped-signal ABI required both route and map row lists to be nonempty even though the frozen producer permits either side to be empty; a validator failure also occurred before the supplementary raw receipt was persisted | Match the frozen combined-nonempty row semantics, bind IDs/content SHA/phase/count independently, and invoke a strict-copy raw-receipt sink before validation | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-013 | P0 | The execution wrapper appended logical decision-evidence references to a validated raw native receipt and then passed the enriched object back through the raw-header ABI | Strip and type-check the exact two storage-enrichment fields before raw ABI validation while preserving the enriched sealed receipt for storage review | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-014 | P0 | The declarative ABI inherited the candidate0-supplementary cross-arm identity flag into Static14D/Scene14D even though the actual CAMP producer never emits that diagnostic-only field | Declare the flag only on the candidate0-supplementary branch and keep method branches exact | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-015 | P0 | The method-branch ABI used shorthand score/tie strings instead of the exact runtime producer values, rejecting the first valid Static14D tick before Scene14D | Bind both CAMP branches to `score_k=clip(a_k/s,0,10)^T w` and `lowest_eligible_candidate_index`; reject the stale shorthand in producer and independent-review tests | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-016 | P0 | The holdout wrapper and reviewer hard-coded 64 decision-evidence rows even though the frozen native callback samples CAMP evidence at `sample_every_ticks=5` | Independently derive exact tick indices from each sealed run config (`0,5,...,60`; candidate0 remains empty) and reject cadence/type/index drift | implementation and AutoDL focused regression complete; production canary pending |
| B4-RC-017 | P0 | The projected complete-row validator rechecked a valid ABI receipt against the historical pre-ABI affine score string after raw validation had already passed | Dispatch the downstream score string by ABI presence, preserving historical reopenability while requiring the exact normalized runtime contract for B4 | implementation and AutoDL focused regression complete; superseded by merged ABI closure |
| B4-RC-018 | P0 | Incremental canaries exposed one ABI literal or branch relation at a time because the mutation suite used hand-built receipts and the consumer inventory covered only two helpers; the no-signal certified safety phase `none` was the remaining reproduced mismatch | Close the four actual-native branches in one change: require certified safety fields, freeze literal domains and cross-branch relations, bind safety phase to controlled-scene phase, run every sealed real branch/variant through producer and independent validators/projectors, and cover missing/extra/type/shape/literal/nested/cross-branch mutations plus execution-review-evaluation call surfaces | complete: sealed execution `731b4789...` and independent execution review `83738d4f...` passed all 9 arms / 576 ticks |
| B4-RC-019 | P0 | The generic evaluation entrypoint prevalidated the production-RC v2 release, then the shared evaluator revalidated it against the legacy `cas_tombstone_path` schema and failed before writing an evaluation artifact | Dispatch RC and legacy schemas without fallback; validate RC scientific-exposure receipts, add evaluation tests to the frozen focused suite, and bind the immutable b637 execution/release/ledger evidence to the new evaluator/reviewer through an exact dual-HEAD, dual-manifest provenance record | complete: sealed evaluation `a0fe76b2...`; replacement canary prohibited |
| B4-RC-020 | P0 | The 3-unit nonFresh lifecycle canary reached the unchanged Fresh claim evaluator, whose seven-family coverage gate correctly rejected the deliberately minimal production-equivalence fixture | Dispatch only the exact `fresh_b4_nonfresh_production_canary` split to a deterministic lifecycle-only no-claim result while retaining full row/statistic reconstruction; all real holdout splits continue to use the frozen Fresh claim/NI/coverage rules | complete: lifecycle-only no-claim evaluation sealed; execution was not rerun |
| B4-RC-021 | P0 | The independent reviewer compared its in-memory `numpy.float64` statistic to the canonically persisted native JSON `float` with exact Python type equality, rejecting a bit-identical value after the evaluation seal | Independently round-trip only the rebuilt value through the frozen canonical JSON boundary before exact comparison; retain strict finite parsing and reject the adjacent representable float | reviewer-only consistency fix complete locally; AutoDL focused suite and the review/certificate chain pending |
| B4-RC-022 | P0 | The B4 pre-open bridge reopened the accepted pre-provenance B3 map-suite receipt with the current receipt validator, which now requires two later generator provenance fields | Accept only the exact historical B3 receipt obtained by removing those two later fields from the current deterministic B3 receipt; preserve its original identity hash and reject every other mutation | historical sealed-authority compatibility fix complete locally; AutoDL suite and B4 pre-open rebuild pending |

The production-equivalence certificate is created only after all critical
implementation work is complete.  After the certificate, critical code is
frozen; only the existing pointer/audit documentation allowlist may change.
