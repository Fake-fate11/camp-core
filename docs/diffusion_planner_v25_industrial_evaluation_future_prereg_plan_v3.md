# Future Preregistration Plan after Industrial Evaluation Amendment v3

This plan authorizes no execution. High/control must separately decide whether
training-support evidence remains relevant, whether adaptation or retraining
requires a prospective decision, or whether a future same-ego batch8
experiment may proceed.

Before any future claim-bearing run:

1. Preserve the exact 161-leaf registry and exact test-family membership.
2. Prospectively assign a finite nonnegative numeric `M_leaf` to every
   noninferiority leaf from an external or separately accepted authority.
   No observed development, validation, B4 or Fresh value may select it.
3. Preserve the direction-oriented paired cluster delta, Student-t p-value,
   zero-variance boundary and stable Holm step-down rule verbatim.
4. Preserve separate hard-safety and guardrail intersection-union layers.
   Missing or failed leaves retain the full denominator and block the relevant
   family. No weighted compensation is allowed. Never create a weighted total.
5. Treat unadjusted 95% intervals as descriptive only; claim decisions use
   the frozen familywise p-value procedure.
6. Keep SafetyCost and its six components as immutable legacy exploratory
   diagnostics only.
7. Instrument missing target-architecture latency, THW, PET, false-stop
   context, contact dynamics and occupant/seat channels only through a new
   prospective nonholdout/holdout authority. Never convert missing evidence
   to zero or a legacy proxy.
8. Maintain same-ego single-invocation batch8 generation, candidate0=row0,
   common immutable pool consumption and zero post-pool model/DP/latent/
   candidate-generation calls if a future architecture experiment is
   authorized.

No model, pool, selector, training, calibration, threshold materialization,
validation, closed-loop, Fresh, holdout, claim, promotion or deployment is
authorized by this document.
